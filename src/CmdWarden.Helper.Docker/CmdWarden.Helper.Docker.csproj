<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>CmdWarden.Helper.Docker</RootNamespace>
    <AssemblyName>docker-credential-cmdwarden</AssemblyName>
    <Description>docker credential helper: get, store, erase, list through the Session Agent gate (#203).</Description>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\CmdWarden.Contracts\CmdWarden.Contracts.csproj" />
  </ItemGroup>
</Project>
