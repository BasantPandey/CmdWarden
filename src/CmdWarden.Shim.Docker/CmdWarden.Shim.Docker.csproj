<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>CmdWarden.Shim.Docker</RootNamespace>
    <AssemblyName>docker</AssemblyName>
    <Description>PATH shim for docker: Authorize via Session Agent, then spawn pinned real binary (optional child DOCKER_AUTH_CONFIG).</Description>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\CmdWarden.Contracts\CmdWarden.Contracts.csproj" />
  </ItemGroup>
</Project>
