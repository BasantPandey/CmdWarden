<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>CmdWarden.Shim.Az</RootNamespace>
    <AssemblyName>az</AssemblyName>
    <Description>PATH shim for az: Authorize via Session Agent, then spawn pinned real entrypoint (gate only).</Description>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\CmdWarden.Contracts\CmdWarden.Contracts.csproj" />
  </ItemGroup>
</Project>
